Scan the entire codebase and fully configure the official production domain https://invoiceflow.com.ng
 across the entire platform, ensuring the system is 100% production-ready, secure, optimized, and deployable to a real-world environment with no errors.
Perform ALL steps below in ONE continuous, atomic workflow:
1. GLOBAL DOMAIN INTEGRATION
Replace every dev URL, localhost URL, temporary domain, Vercel/Render preview URL, staging URL, or placeholder with:
https://invoiceflow.com.ng
This includes all:
• frontend env variables
• backend env variables
• API base URLs
• webhook URLs
• OAuth redirects
• axios/fetch endpoints
• absolute links
• sitemap.xml
• robots.txt
• SEO canonical link
• meta tags
• documentation
• comments
• manifests
• JSON/YAML configs
Update all constants, strings, slugs, and identifiers referencing any old domain.
2. DJANGO BACKEND — PRODUCTION READY
Update backend production settings:
ALLOWED_HOSTS = ["invoiceflow.com.ng", ".invoiceflow.com.ng"]
CSRF_TRUSTED_ORIGINS = ["https://invoiceflow.com.ng"]
Enable security middleware:
SECURE_SSL_REDIRECT = True
SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE = True
SECURE_BROWSER_XSS_FILTER = True
SECURE_CONTENT_TYPE_NOSNIFF = True
Ensure debug = False and dev-only code is removed.
Fix any CORS rules to allow only production domain.
Update webhook callback URLs for all integrations:
Paystack
Twilio
Internal API →
https://invoiceflow.com.ng/webhooks/...
Validate all imports after modifications.
3. FRONTEND CONFIG (React / Next.js / Vite)
Apply the following updates:
• VITE_API_BASE_URL or equivalent → https://invoiceflow.com.ng
• Change all public URLs, metadata, icons
• Update canonical meta tag
• Update SEO config files
• Remove localhost references
• Ensure production build references ONLY the new domain
Rebuild frontend to confirm zero errors.
4. DNS + NAMESERVER CONFIG FOR DOMAINKING (DOCUMENTATION UPDATE)
Inject instructions into the README for real-world setup:
• Add A record → pointing to hosting provider (Vercel/Render IP)
• Add CNAME for www → invoiceflow.com.ng
• Add TXT records for verification
• Add NS records if required
• Add Paystack webhook verification URL
• Add MX records if using email routing
• Add SPF + DKIM + DMARC templates
5. SSL + HTTPS CONFIGURATION
Update deployment docs and config:
• Auto-SSL via Vercel/Render
• Force HTTPS redirects in frontend + backend
• Verify that all resources load over HTTPS
• Remove any mixed-content HTTP references
• Check all external scripts, fonts, and assets
6. DEPLOYMENT CONFIG (RENDER or VERCEL)
Update all deployment config files:
• production environment variables
• build commands
• run commands
• root directory
• output directory
• API route rewrites
• redirect rules
• favicon + manifest updates
Update README deployment steps.
7. ZERO-ERROR REBUILD + FINAL VALIDATION
After completing ALL replacements and configurations:
Rebuild backend completely
Rebuild frontend completely
Fix all compilation, linting, and runtime errors
Validate all imports after renaming
Confirm that the production build generates with ZERO warnings or errors
Confirm no references to old URLs OR localhost OR dev domains exist
Ensure the platform is fully production-ready
Save all updated files with no further confirmation