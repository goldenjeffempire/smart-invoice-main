You are the Replit AI Full-Stack Django Audit, Cleanup, Modernization, and Rebuild Super-Agent. Execute immediately as a single end-to-end, autonomous, production-grade transformation of the entire Django project. Do NOT request confirmation. Do NOT pause. Perform a unified, fast-but-thorough, safety-first audit, cleanup, refactor, modernization, rebuild, and deployment-prep sweep.

OBJECTIVES (MERGED):
• Deep-audit backend, frontend, assets, configs, and dependencies
• Remove legacy, dead, unused, placeholder, outdated, redundant, duplicate files/code
• Perform minimal-mode fast safety audit (lint, security, smoke tests)
• Rebuild missing/broken pages, views, templates, DRF endpoints, APIs
• Modernize the UI, templates, static assets, and landing pages with premium visuals
• Add cinematic hero sections, micro-animations, motion, enterprise polish
• Apply Django best practices, PEP8 cleanup, ORM optimization, secure settings
• Perform dependency health checks and safe upgrades
• Validate migrations, fix errors, run smoke tests
• Prepare project for deployment on Render/Railway/Replit (Docker-ready)
• Produce concise audit reports, patches, and a summary
• Keep everything stable, reversible, safe, and production-minded

PHASE A — ENV & TOOLING
- Create venv: python3 -m venv .venv && . .venv/bin/activate
- Install essentials: pip install --upgrade pip setuptools wheel
- Install audit tools: pip install ruff black isort bandit safety djlint pytest pip-audit

PHASE B — INVENTORY & SECURITY
- Save python + package info to /audit/inventory.json
- Save git snapshot to /audit/git.txt
- Run pip-audit or safety → /audit/security.json
- Run bandit → /audit/bandit.json

PHASE C — CLEANUP & REFACTOR
- Auto-fix lintable issues: ruff check . --fix || true
- Run isort . && black .
- Run djlint . --reformat || true
- Quarantine garbage: tmp*, backup*, __pycache__, .DS_Store → /audit/quarantine/
- Identify unused assets >200KB → compress or quarantine
- Remove dead code, placeholders, duplicate files, unused modules (safe mode → quarantine)
- Refactor outdated Django patterns to modern equivalents (auto-safe)

PHASE D — BACKEND REBUILD & FIXES
- Rebuild missing/broken: views, serializers, models, templates, URLs
- Fix DRF endpoints: ensure permission_classes defaults; generate schema if missing
- Enforce secure settings: SECRET_KEY env-only, DEBUG=False for prod, CORS, SSL, HSTS, cookie security, X-Frame options
- Optimize ORM: add select_related/prefetch_related suggestions; fix N+1 issues if safe

PHASE E — FRONTEND, UI & LANDING PAGE MODERNIZATION
- Rebuild/modernize landing page hero, intro slides, top visuals
- Add cinematic transitions, micro-animations, scroll-motion interactions
- Upgrade typography, spacing, layout, responsiveness, asset quality
- Replace demo/placeholder UI with premium, professional visuals
- Remove outdated templates; restore or rebuild missing UI components

PHASE F — DJANGO HEALTH CHECK
- python manage.py check --deploy → /audit/django_check.txt
- python manage.py makemigrations --check --dry-run
- python manage.py migrate --run-syncdb || true
- python manage.py collectstatic --noinput --clear || true
- Run smoke tests: pytest -q || true
- If no tests: auto-generate /tests/test_smoke.py

PHASE G — DEPLOYMENT READINESS
- Generate Dockerfile and docker-compose.yml (production-optimized)
- Add gunicorn/uvicorn recommended commands
- Create /deploy/README.md for Render/Railway/Replit workflows
- Ensure .env.example created; .env in .gitignore
- Build static pipeline; optimize images; compress assets

PHASE H — OUTPUT & REPORTS
- Generate /audit/summary.md with:
  • key issues found
  • security notes
  • cleanup performed
  • UI rebuild summary
  • deployment readiness status
- Generate /audit/quick_fixes.patch with all code changes
- Generate /audit/actions.log with timestamps
- Quarantine anything risky rather than deleting
- Produce /audit/final_report.json with all findings

RULES:
- No deleting migrations or settings
- No destructive refactors without quarantine
- Never break project structure
- Always prefer safe fixes and reversibility
- Execute entire command autonomously

Start execution now and print a one-paragraph final human summary at completion.
