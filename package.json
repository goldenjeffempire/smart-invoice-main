{
  "name": "workspace",
  "version": "1.0.0",
  "description": "Smart, Simple Invoicing for modern businesses. Create professional invoices in minutes, send via email or WhatsApp, and get paid faster.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "build:css": "tailwindcss -i ./static/css/tailwind.input.css -o ./static/css/tailwind.output.css --minify",
    "watch:css": "tailwindcss -i ./static/css/tailwind.input.css -o ./static/css/tailwind.output.css --watch",
    "minify:js": "terser static/js/app.js -o static/js/app.min.js -c -m --source-map",
    "minify:css": "cssnano static/css/main.css static/css/main.min.css && cssnano static/css/enterprise-design-system.css static/css/enterprise-design-system.min.css && cssnano static/css/internal-pages.css static/css/internal-pages.min.css",
    "optimize:images": "imagemin static/images/*.jpg --out-dir=static/images --plugin=webp",
    "build:prod": "npm run build:css && npm run minify:js && npm run minify:css",
    "build": "npm run build:prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.18"
  },
  "dependencies": {
    "cssnano": "^7.1.2",
    "cssnano-cli": "^1.0.5",
    "imagemin-cli": "^8.0.0",
    "imagemin-webp": "^8.0.0",
    "terser": "^5.44.1"
  }
}
